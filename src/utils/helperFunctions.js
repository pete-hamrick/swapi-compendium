export function filterCharactersByEyeColor(characterList, eyeColor) {
  // There may be some bug here when I bring in previous and next buttons
  // may need to add a current url to state?
  // initialize empty array for sorted characters
  // need to loop through character array, maybe map?
  // if eye_color === eyeColor
  // push character into sorted characters array
  // return sorted characters array
}
